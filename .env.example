# Copia este archivo a .env:
#   cp .env.example .env
#
# Supabase → Project Settings → API
#   Project URL     → VITE_SUPABASE_URL
#   anon / public   → VITE_SUPABASE_ANON_KEY
#
# Setup completo: ver README.md (SQL: supabase/cuptrack.sql)

VITE_SUPABASE_URL=https://TU_PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key_aqui

# Pitch / demos (switcher de cafés + link Staff). En producción: false
VITE_DEMO=true

# Solo modo local (sin Supabase). No usar en producción.
VITE_ADMIN_PIN=cambiar-este-pin
